# Copyright (c) 2018-2024, Firas Khalil Khana
# Distributed under the terms of the ISC License

# Voyager: Firas Khalil Khana (firasuke) <firasuke@glaucuslinux.org>

prepare() {
  autoreconf -vfis
}

configure() {
  glaucus-configure \
    --disable-static \
    --with-pic \
    --with-pkg-config-dir=/usr/lib/${nom}ig \
    --with-system-libdir=/usr/lib \
    --with-system-includedir=/usr/include
}

build() {
  make
}

check() {
  make \
    check
}

package() {
  make \
    DESTDIR="$SACD" \
    install-strip

  # Compatibility symlinks
  ln -fnrsv $nom "$SACD"/usr/bin/pkg-config
  ln -fnrsv $nom "$SACD"/usr/bin/$TGT-$nom
  ln -fnrsv $nom "$SACD"/usr/bin/$TGT-pkg-config

  # Remove libtool archive
  rm -fv "$SACD"/usr/lib/lib$nom.la

  ln -fnrsv pkg.m4 "$SACD"/usr/share/aclocal/$nom.m4
}
