# Copyright (c) 2018-2023, Firas Khalil Khana
# Distributed under the terms of the ISC License

# Voyager: Firas Khalil Khana (firasuke) <firasuke@glaucuslinux.org>

prepare() {
  $RSYNC $SRCD/$nom/$nom-$ver/ $SSRC/$nom --delete
  cd $SSRC/$nom

  $PATCH -p1 -i $CERD/$nom/patches/clear/multi-thread-default.patch

  $PATCH -p1 -i $CERD/$nom/patches/opensuse/p${nom}.1.patch
}

configure() {
  :
}

build() {
  $MAKE \
    HAVE_PTHREAD=1 \
    ZSTD_LEGACY_SUPPORT=0 \
    HAVE_ZLIB=1 \
    HAVE_LZMA=1 \
    HAVE_LZ4=1 \
    BACKTRACE=0

  $MAKE \
    lib-mt

  # Build parallel zstd as well (requires `-fexceptions`)
  CXXFLAGS="$(echo $CXXFLAGS | sed 's/-fno-exceptions //')" \
  $MAKE \
    -C contrib/p$nom
}

check() {
  :
}

install() {
  $MAKE \
    DESTDIR=$VNMD/$nom/sac \
    PREFIX=/usr \
    install

  # Install parallel zstd as well
  $MAKE \
    DESTDIR=$VNMD/$nom/sac \
    PREFIX=/usr \
    -C contrib/p$nom \
    install

  # Manually remove the static library
  $RM $VNMD/$nom/sac/usr/lib/lib$nom.a

  $LN $nom.1 $VNMD/$nom/sac/usr/share/man/man1/p${nom}.1
}
